__pycache__/
*.pyc
*.db
*.pkl
.pytest_cache/
*.egg-info/
.env

# Private data — interaction logs, shadow output, quality scores, trained models
data/interactions/
data/shadow-log.jsonl
data/quality-state.json
data/harvester-state.json
data/shadow-report.json
data/gp-tagger.pkl
plugin/node_modules/

# Rendered launchd plists (contain local paths) — use service/*.plist.template + install-service.sh
service/*.plist

# Live runtime state — grows with usage, contains emergent tags from real conversations
data/tag_registry.json

data/store.db-shm
data/store.db-wal

# Doc indexer runtime artifacts
data/doc_indexer.log
data/doc_graph.db-shm
data/doc_graph.db-wal
