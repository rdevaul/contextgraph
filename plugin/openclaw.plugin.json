{
  "id": "contextgraph",
  "name": "Context Graph Engine",
  "version": "1.0.0",
  "description": "Graph-based context engine that bridges OpenClaw's ContextEngine interface to the contextgraph Python FastAPI server. Enables semantic topic-tagged context assembly instead of linear windowing.",
  "openclaw": {
    "extensions": ["./index.ts"]
  },
  "configSchema": {
    "type": "object",
    "properties": {},
    "additionalProperties": true
  }
}
