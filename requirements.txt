# Core (stdlib only for store, features fallback, tagger, assembler, cli)
# Optional: install for better NLP in features.py
spacy>=3.7
# python -m spacy download en_core_web_sm

# Summarization
anthropic>=0.40
requests>=2.31  # for Ollama backend

# Fixed tagger (YAML config)
pyyaml>=6.0

# Testing
pytest>=8.0

# Optional: GP tagger (experimental, CONTEXTGRAPH_TAGGER_MODE=hybrid or gp-only)
# ⚠️  See config.py for voting limitation — GP cannot promote tags on its own
#    with default threshold. Not recommended for production use.
# deap>=1.4

# Future
# sentence-transformers>=3.0
